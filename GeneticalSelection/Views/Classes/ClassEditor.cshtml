@model Class

<div class="row p-2">
    <div class="col">
        <input asp-for="Name" class="form-control" />
    </div>
    <div class="col">
        <input asp-for="LatinName" class="form-control" />
    </div>
    <div class="col">
        <input asp-for="Description" class="form-control" />
    </div>
    <div class="col">
        <select asp-for="SubphylumId" class="form-control ml-1 mr-1">
            @foreach (Subphylum sp in ViewBag.Subphylums)
            {
                <option value="@sp.Id" selected="@(Model.SubphylumId == sp.Id)">@sp.Name</option>
            }
        </select>
    </div>
    <div class="col-3">
        @if (Model.Id == 0)
        {
            <button type="submit" class="btn btn-primary">Add</button>
        }
        else
        {
            <button type="submit" class="btn btn-outline-primary">Save</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
        }
    </div>
</div>